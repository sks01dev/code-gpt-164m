# code-gpt-164m
A 162.5M parameter decoder-only Causal Language Model built from scratch in PyTorch following GPT-2 Small specifications, featuring FP16 AMP training and custom $O(1)$ KV-Cache inference acceleration.
